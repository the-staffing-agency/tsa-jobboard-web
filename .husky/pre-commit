#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running type check..."
pnpm type-check

echo "🎨 Running linter and formatter on staged files..."
pnpm lint:staged

echo "✅ Pre-commit checks passed!"